(function(){
const $=s=>document.querySelector(s),terms=['writing','design','marketing','research','analytics','data','communication','sales','video','ai','project management','social media','content creation','excel','organization','customer service','problem solving','seo','advertising','reporting','strategy'];
const read=(k,fallback={})=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(fallback))}catch{return fallback}};
const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
document.addEventListener('DOMContentLoaded',()=>{const form=$('#job-match-form');if(!form)return;const saved=read('careerToolkit');if(saved.jobText)$('#job-description').value=saved.jobText;form.onsubmit=e=>{e.preventDefault();const job=$('#job-description').value.toLowerCase(),title=$('#job-title').value||'This role',profile=read('careerProfile',{skills:[]}),toolkit=read('careerToolkit'),cv=(toolkit.cvText||'').toLowerCase(),required=terms.filter(x=>job.includes(x)),careerHave=required.filter(x=>(profile.skills||[]).some(s=>s.includes(x)||x.includes(s))),cvHave=required.filter(x=>cv.includes(x)),missing=required.filter(x=>!cvHave.includes(x)),careerScore=required.length?Math.round(45+50*careerHave.length/required.length):60,cvScore=cv?Math.round(45+50*cvHave.length/Math.max(required.length,1)):0,atsScore=cv?Math.round((cvScore+Math.min(90,55+(cv.match(/\n/g)||[]).length*2))/2):0,verdict=Math.max(careerScore,cvScore)>=78?'Strong potential match — consider applying.':Math.max(careerScore,cvScore)>=62?'Potential match — address the gaps before applying.':'Build stronger evidence before prioritizing this application.',root=$('#job-results');toolkit.jobText=$('#job-description').value;localStorage.setItem('careerToolkit',JSON.stringify(toolkit));root.classList.add('show');root.innerHTML=`<div class="results-head"><span class="kicker">Integrated match report</span><h2>${esc(title)}</h2></div><div class="score-grid"><div class="score-box"><h3>Career Match</h3><strong>${careerScore}%</strong></div><div class="score-box"><h3>CV Match</h3><strong>${cv?cvScore+'%':'Not added'}</strong></div><div class="score-box"><h3>ATS Match</h3><strong>${cv?atsScore+'%':'Not added'}</strong></div></div><article class="result-card"><div><h3>Should you apply?</h3><p><strong>${verdict}</strong></p><h3>Supported matches</h3><div class="skill-row">${(cvHave.length?cvHave:careerHave.length?careerHave:['transferable experience']).map(x=>`<span class="skill-chip">${esc(x)} ✓</span>`).join('')}</div><h3>Missing keywords or evidence</h3><p>${missing.length?missing.map(esc).join(' · '):'No obvious tracked keyword gaps. Review required experience and evidence manually.'}</p><p>This rule-based report compares explicit language. It is not a hiring or ATS guarantee.</p><div class="result-actions"><a class="button" href="/cv-optimizer">Optimize My CV</a><a class="button secondary" href="/cover-letter">Generate Cover Letter</a><a class="button secondary" href="/interview-prep">Prepare for Interview</a></div></div></article>`;root.scrollIntoView({behavior:'smooth'})}});
})();
